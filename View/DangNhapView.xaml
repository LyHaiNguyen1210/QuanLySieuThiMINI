<Window x:Class="QuanLySieuThiMINI.View.DangNhapView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:QuanLySieuThiMINI.ViewModel"
      mc:Ignorable="d" 
      d:DesignHeight="500" d:DesignWidth="450"
      Title="DangNhapView" Background="#F4F6F8">
    <Window.DataContext>
        <local:DangNhapViewModel/>
    </Window.DataContext>
    <Grid>
        <Border Background="White" CornerRadius="12" Padding="35,40" Margin="20"
                HorizontalAlignment="Center" VerticalAlignment="Center" Width="380">

            <Border.Effect>
                <DropShadowEffect BlurRadius="15" ShadowDepth="2" Color="#D3D3D3" Opacity="0.5"/>
            </Border.Effect>

            <StackPanel>
                <TextBlock Text="ĐĂNG NHẬP" FontSize="26" FontWeight="Bold" Foreground="#2C3E50" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                <TextBlock Text="Hệ thống Quản lý Siêu thị Mini" FontSize="13" Foreground="#7F8C8D" HorizontalAlignment="Center" Margin="0,0,0,35"/>

                <TextBlock Text="👤 Tên đăng nhập" FontWeight="SemiBold" Foreground="#34495E" Margin="0,0,0,5"/>
                <TextBox Text="{Binding MaNV, UpdateSourceTrigger=PropertyChanged}" Height="40" VerticalContentAlignment="Center" Padding="10,0" Margin="0,0,0,20" FontSize="14" BorderBrush="#BDC3C7" BorderThickness="1">
                    <TextBox.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="4"/>
                        </Style>
                    </TextBox.Resources>
                </TextBox>

                <TextBlock Text="🔒 Mật khẩu" FontWeight="SemiBold" Foreground="#34495E" Margin="0,0,0,5"/>
                <PasswordBox x:Name="txtPassword" Height="40" VerticalContentAlignment="Center" Padding="10,0" Margin="0,0,0,10" FontSize="14" BorderBrush="#BDC3C7" BorderThickness="1">
                    <PasswordBox.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="4"/>
                        </Style>
                    </PasswordBox.Resources>
                </PasswordBox>
                <TextBlock Text="{Binding ThongBao}" Foreground="Red" HorizontalAlignment="Center" Margin="0,0,0,10" TextWrapping="Wrap"/>

                
                <Button Command="{Binding DangNhapCommand}" CommandParameter="{Binding ElementName=txtPassword}" Content="ĐĂNG NHẬP" Height="45" Background="#2980B9" Foreground="White" FontSize="15" FontWeight="Bold" BorderThickness="0" Cursor="Hand">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="6"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </Border>
    </Grid>
</Window>
